<UserControl x:Class="Othello.view.TopActionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Othello.view"
             mc:Ignorable="d" 
             Height="100"
             d:DesignHeight="300" >
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
        <Button Width="100" Margin="20,20,10,20" Command="{Binding UndoCommand}" Content="Undo"/>
        <Button Width="100" Margin="10,20,10,20" Command="{Binding ResetCommand}" Content="Reset"/>
        <Button Width="100" Margin="10,20,10,20" Command="{Binding SaveCommand}" Content="Save"/>
        <Button Width="100" Margin="10,20,20,20" Command="{Binding LoadCommand}" Content="Load"/> 
    </StackPanel>
    
</UserControl>
