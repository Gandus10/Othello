<UserControl x:Class="Othello.view.GameInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Othello.view"
             mc:Ignorable="d" 
             Width="300"
             d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Vertical" Background="White" Opacity="0.5">
            <TextBlock Foreground="Black" HorizontalAlignment="Center" Text="{Binding Player1.Name}"/>
            <TextBlock Text="Score:"/>
            <TextBlock Text="{Binding Player1.Score}"/>
            <TextBlock Text="Temps écoulé:"/>
            <TextBlock Text="{Binding Player1.TimeElapsed}"/>
            <TextBlock Margin="0,80,0,0" Foreground="Black" HorizontalAlignment="Center" Text="{Binding Player2.Name}"/>
            <TextBlock Text="Score:"/>
            <TextBlock Text="{Binding Player2.Score}"/>
            <TextBlock Text="Temps écoulé:"/>
            <TextBlock Text="{Binding Player2.TimeElapsed}"/>
        </StackPanel>
    </Grid>
</UserControl>
